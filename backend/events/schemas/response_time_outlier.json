{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "response_time_outlier properties",
  "type": "object",
  "required": ["task_id", "response_time_ms", "expected_ms", "z_score"],
  "properties": {
    "task_id": {"type": "integer"},
    "response_time_ms": {"type": "integer", "minimum": 0},
    "expected_ms": {"type": "integer", "minimum": 1},
    "z_score": {"type": "number"},
    "outlier_type": {"type": "string", "enum": ["too_fast", "too_slow"]}
  },
  "additionalProperties": true
}
