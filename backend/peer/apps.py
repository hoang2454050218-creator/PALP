from django.apps import AppConfig


class PeerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "peer"
    verbose_name = "Peer Engine (anti-herd, reciprocal teaching)"
